aiogram==3.3.0
asyncpg==0.27.0
python-dotenv==1.0.0
aiohttp==3.8.4
APScheduler==3.10.1
# Pin pydantic to a version compatible with newer Python runtimes.
# If Railway uses Python 3.13 and pydantic-core/build fails, prefer setting Python runtime to 3.12.
pydantic>=2.9.0
